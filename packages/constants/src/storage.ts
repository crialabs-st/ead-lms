export const STORAGE_BUCKETS = {
  storage: {
    keys: {
      studentProgress: 'course_student_progress',
      annotations: 'course_annotations',
      preferences: 'course_preferences',
      documents: 'users_documents',
    }
  }
} as const;