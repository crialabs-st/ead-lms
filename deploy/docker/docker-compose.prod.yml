# Docker Compose for local production testing
# Test your production Docker build locally before deploying to Railway
#
# Usage:
#   docker compose -f deploy/docker/docker-compose.prod.yml up --build
#   docker compose -f deploy/docker/docker-compose.prod.yml down
#   docker compose -f deploy/docker/docker-compose.prod.yml down -v  # Remove volumes
#
# All values have sensible defaults. Override with environment variables:
#   POSTGRES_PASSWORD=secret COOKIE_SECRET=xxx BETTER_AUTH_SECRET=xxx BETTER_AUTH_URL=http://localhost:8080 docker compose -f deploy/docker/docker-compose.prod.yml up

services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres_prod_password}
      POSTGRES_DB: ${POSTGRES_DB:-app_prod}
    ports:
      - '${POSTGRES_PORT:-5433}:5432'
    volumes:
      - postgres_prod_data:/var/lib/postgresql/data
    healthcheck:
      test:
        [
          'CMD-SHELL',
          'pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-app_prod}',
        ]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    build:
      context: ../..
      dockerfile: apps/api/Dockerfile
    restart: unless-stopped
    ports:
      - '${API_PORT:-8080}:8080'
    environment:
      NODE_ENV: ${NODE_ENV:-production}
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres_prod_password}@postgres:5432/${POSTGRES_DB:-app_prod}
      API_URL: ${API_URL:-http://localhost:8080}
      FRONTEND_URL: ${FRONTEND_URL:-http://localhost:3000}
      BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET:-change-this-to-a-long-random-secret-min-32-chars}
      BETTER_AUTH_URL: ${BETTER_AUTH_URL:-http://localhost:8080}
      PORT: 8080
      LOG_LEVEL: ${LOG_LEVEL:-normal}
      COOKIE_SECRET: ${COOKIE_SECRET:-your-secret-key-change-in-production}
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test:
        [
          'CMD',
          'node',
          '-e',
          "require('http').get('http://localhost:8080/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})",
        ]
      interval: 30s
      timeout: 3s
      start_period: 40s
      retries: 3
    volumes:
      - api_uploads_data:/app/apps/api/public/uploads

  web:
    build:
      context: ../..
      dockerfile: apps/web/Dockerfile
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:8080/api}
    restart: unless-stopped
    ports:
      - '${WEB_PORT:-3000}:3000'
    environment:
      NODE_ENV: ${NODE_ENV:-production}
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test:
        [
          'CMD',
          'node',
          '-e',
          "require('http').get('http://localhost:3000', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})",
        ]
      interval: 30s
      timeout: 3s
      start_period: 30s
      retries: 3

volumes:
  postgres_prod_data:
  api_uploads_data:
